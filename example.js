

// sorted keys
// wildcards
// console output
// mocker
// async
// mocker + async

// You don't forget to write tests
// You err on overtesting instead of undertesting
//   hidden attributes
//   hidden this


/*
// Some setup
$ base = doctest.params.base
>   || 'https://www.googleapis.com/freebase/v1'
$ print(base);
...

$.ajax({
  url: base + '/search',
  data: {query: 'nirvana'},
  success: Spy('search', {wait: true})
});

/* => {...} */

// features:
//
